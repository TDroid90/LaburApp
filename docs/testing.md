# Pruebas

`npm test` cubre comisión, rangos, transición de estados, elegibilidad de reseña y detección de contacto. `npm run test:e2e` recorre el camino feliz del dominio desde solicitud hasta liberación y reseña. CI ejecuta lint, tipos, pruebas y build del panel.
