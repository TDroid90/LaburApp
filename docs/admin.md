# Administración

El panel local muestra métricas, revisión y estados con datos ficticios. En producción cada ruta debe validar sesión y rol en el servidor, y cada mutación debe crear auditoría con actor, motivo, estado anterior y posterior. Ocultar enlaces en el cliente no es autorización.
